# TalabaGo v2.0 — Railway/Nixpacks
# Unified: backend/ va frontend/ yo'q

[phases.setup]
nixPkgs = ["python311", "nodejs_20", "gcc"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "npm ci"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "bash start.sh"
